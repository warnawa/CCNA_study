# Тиждень 11-12: Безпека мереж

## 📚 Мета модуля
Отримати розуміння основ безпеки мереж, навчитися захищати мережеві пристрої, налаштовувати ACL, SSH та Port Security для забезпечення безпеки мережевої інфраструктури.

**Очікувані результати:**
- Розуміти основні загрози безпеці мереж
- Знати типи атак та способи захисту
- Можливість налаштувати базові ACL
- Розуміти безпеку пристроїв
- Можливість налаштувати SSH та Port Security

---

## 📖 Тема 6.1: Основи безпеки

### 6.1.1 Загрози безпеці мереж

**Мережева безпека** - захист мережевої інфраструктури від несанкціонованого доступу, атак та інших загроз.

#### Основні принципи безпеки (CIA Triad):

**1. Confidentiality (Конфіденційність):**
- Захист даних від несанкціонованого доступу
- Шифрування даних
- Контроль доступу

**2. Integrity (Цілісність):**
- Забезпечення того, що дані не змінені
- Виявлення змін
- Захист від модифікації

**3. Availability (Доступність):**
- Забезпечення доступності систем та даних
- Захист від DoS атак
- Резервування та відмовостійкість

#### Типи загроз:

**1. Зовнішні загрози:**
- Хакери
- Віруси та malware
- DoS/DDoS атаки
- Фішинг

**2. Внутрішні загрози:**
- Необережні працівники
- Зловмисні інсайдери
- Втрачені пристрої
- Неправильна конфігурація

**3. Фізичні загрози:**
- Крадіжка обладнання
- Фізичне пошкодження
- Несанкціонований доступ до приміщень

---

### 6.1.2 Типи атак

#### 1. DoS (Denial of Service) / DDoS (Distributed DoS)

**Визначення:**
- Атака, спрямована на недоступність сервісу
- Перевантаження ресурсів (CPU, пам'ять, пропускна здатність)
- DDoS - атака з багатьох джерел одночасно

**Типи DoS атак:**

**a) Flood атаки:**
- SYN Flood
- ICMP Flood (Ping Flood)
- UDP Flood

**b) Amplification атаки:**
- Використання сервісів для збільшення трафіку
- DNS Amplification
- NTP Amplification

**Захист:**
- Firewall
- Rate limiting
- DDoS protection сервіси

#### 2. MITM (Man-in-the-Middle)

**Визначення:**
- Атакуючий перехоплює комунікацію між двома сторонами
- Може читати та модифікувати дані
- Сторони не підозрюють про присутність атакуючого

**Приклади:**
- ARP Spoofing
- DNS Spoofing
- SSL Stripping

**Захист:**
- Шифрування (HTTPS, VPN)
- Certificate validation
- ARP protection

#### 3. Malware (Шкідливе програмне забезпечення)

**Типи:**

**a) Віруси:**
- Прикріпляються до файлів
- Розмножуються

**b) Черв'яки (Worms):**
- Самостійно розповсюджуються
- Використовують уразливості мережі

**c) Троянські коні:**
- Маскуються під корисне ПЗ
- Відкривають backdoor

**d) Ransomware:**
- Шифрує дані
- Вимагає викуп

**Захист:**
- Antivirus
- Firewall
- Оновлення систем
- Обізнаність користувачів

#### 4. Social Engineering

**Визначення:**
- Маніпуляція людьми для отримання інформації
- Використання людської психології

**Типи:**

**a) Фішинг:**
- Підроблені email/веб-сайти
- Вимагання конфіденційної інформації

**b) Vishing (Voice Phishing):**
- Телефонні дзвінки
- Видавання себе за службову особу

**c) Pretexting:**
- Створення вигаданого сценарію
- Збір інформації

**Захист:**
- Навчання персоналу
- Політики безпеки
- Двофакторна аутентифікація

#### 5. Password Attacks

**Типи:**

**a) Brute Force:**
- Перебір всіх можливих паролів
- Повільний, але ефективний

**b) Dictionary Attack:**
- Використання словника паролів
- Швидший за brute force

**c) Rainbow Tables:**
- Попередньо обчислені хеші
- Швидке зламування хешів

**Захист:**
- Складні паролі
- Обмеження спроб входу
- Account lockout
- Двофакторна аутентифікація

---

### 6.1.3 Файрволи (Firewalls) - основи

**Firewall** - пристрій або програмне забезпечення, яке контролює вхідний та вихідний мережевий трафік.

#### Типи файрволів:

**1. Packet Filtering Firewall:**
- Перевіряє заголовки пакетів
- Швидкий, але обмежений
- Рівень 3-4 OSI

**2. Stateful Firewall:**
- Відстежує стан з'єднань
- Більш безпечний
- Розуміє контекст трафіку

**3. Application Layer Firewall:**
- Перевіряє вміст пакетів
- Найбезпечніший, але повільніший
- Рівень 7 OSI

**4. Next-Generation Firewall (NGFW):**
- Комбінує різні технології
- Deep Packet Inspection
- Intrusion Prevention System (IPS)

#### Правила файрволу:

**Типові правила:**
```
Allow: Дозволити трафік
Deny: Заборонити трафік
Log: Логувати події
```

**Приклад правил:**
```
1. Deny all incoming traffic (за замовчуванням)
2. Allow HTTP (port 80) from internet to web server
3. Allow HTTPS (port 443) from internet to web server
4. Allow SSH (port 22) from management network only
5. Allow all outgoing traffic
```

#### Zones (Зони):

**Типові зони:**
- **Inside:** Внутрішня мережа (довіряється)
- **Outside:** Зовнішня мережа (не довіряється)
- **DMZ:** Демілітаризована зона (частково довіряється)

---

### 6.1.4 ACL (Access Control Lists) - основи

**ACL** - список правил, який контролює, який трафік дозволено або заборонено.

#### Типи ACL:

**1. Standard ACL:**
- Фільтрує тільки по Source IP
- Номер: 1-99, 1300-1999
- Простіший, але менш гнучкий

**2. Extended ACL:**
- Фільтрує по Source IP, Destination IP, Protocol, Port
- Номер: 100-199, 2000-2699
- Більш гнучкий та точний

**3. Named ACL:**
- Іменовані ACL (замість номерів)
- Легше керувати
- Можна редагувати

#### Напрямки ACL:

**Inbound (Вхідний):**
- Трафік, який надходить на інтерфейс
- Перевіряється перед маршрутизацією

**Outbound (Вихідний):**
- Трафік, який виходить з інтерфейсу
- Перевіряється після маршрутизації

#### Порядок правил:

**Важливо:**
- ACL перевіряється зверху вниз
- Перше збігнення застосовується
- Implicit deny в кінці (все інше заборонено)

#### Налаштування Standard ACL:

**Синтаксис:**
```cisco
access-list <number> {permit | deny} <source> [wildcard-mask]
```

**Приклад:**
```cisco
! Заборонити весь трафік з 192.168.1.0/24
access-list 10 deny 192.168.1.0 0.0.0.255

! Дозволити весь інший трафік
access-list 10 permit any

! Застосувати на інтерфейс
interface FastEthernet0/0
ip access-group 10 in
```

#### Налаштування Extended ACL:

**Синтаксис:**
```cisco
access-list <number> {permit | deny} <protocol> <source> <destination> [operator port]
```

**Приклад:**
```cisco
! Дозволити HTTP з будь-якої мережі до 192.168.1.10
access-list 100 permit tcp any host 192.168.1.10 eq 80

! Дозволити SSH тільки з 192.168.1.0/24
access-list 100 permit tcp 192.168.1.0 0.0.0.255 host 192.168.1.10 eq 22

! Заборонити все інше
access-list 100 deny ip any any

! Застосувати на інтерфейс
interface FastEthernet0/0
ip access-group 100 in
```

#### Named ACL:

**Приклад:**
```cisco
ip access-list extended WEB_SERVER
permit tcp any host 192.168.1.10 eq 80
permit tcp any host 192.168.1.10 eq 443
deny ip any any

interface FastEthernet0/0
ip access-group WEB_SERVER in
```

#### Оператори портів:

- **eq:** Дорівнює (equal)
- **gt:** Більше (greater than)
- **lt:** Менше (less than)
- **neq:** Не дорівнює (not equal)
- **range:** Діапазон

**Приклад:**
```cisco
access-list 100 permit tcp any any eq 80
access-list 100 permit tcp any any range 8000 8010
```

#### Перевірка ACL:

```cisco
# Перегляд ACL
show access-lists

# Перегляд застосованих ACL
show ip access-lists

# Перегляд інтерфейсів з ACL
show ip interface
```

---

### 6.1.5 VPN (Virtual Private Network) - основи

**VPN** - віртуальна приватна мережа, яка створює зашифроване з'єднання через публічну мережу.

#### Призначення:

- Безпечний доступ до корпоративної мережі
- Шифрування даних
- Анонімність
- Обхід географічних обмежень

#### Типи VPN:

**1. Site-to-Site VPN:**
- З'єднання між двома мережами
- Постійне з'єднання
- Використовується для філій

**2. Remote Access VPN:**
- З'єднання окремого користувача з мережею
- Тимчасове з'єднання
- Використовується для віддалених працівників

#### Протоколи VPN:

**1. IPsec (IP Security):**
- Найпоширеніший
- Забезпечує шифрування та аутентифікацію
- Використовується для Site-to-Site

**2. SSL/TLS VPN:**
- Використовує HTTPS
- Простіший у налаштуванні
- Використовується для Remote Access

**3. PPTP (Point-to-Point Tunneling Protocol):**
- Застарілий
- Менш безпечний
- Не рекомендується

#### Компоненти IPsec:

**1. AH (Authentication Header):**
- Аутентифікація та цілісність
- Не шифрує дані

**2. ESP (Encapsulating Security Payload):**
- Шифрування та аутентифікація
- Найпоширеніший

**3. IKE (Internet Key Exchange):**
- Обмін ключами
- Встановлення безпечного з'єднання

---

## 📖 Тема 6.2: Безпека пристроїв

### 6.2.1 Паролі та їх захист

#### Типи паролів на Cisco:

**1. Enable Password:**
- Пароль для режиму enable
- Зберігається в plain text (небезпечно)

**2. Enable Secret:**
- Пароль для режиму enable
- Зберігається в зашифрованому вигляді (безпечніше)
- Має пріоритет над enable password

**3. Console Password:**
- Пароль для консольного доступу

**4. VTY Password:**
- Пароль для Telnet/SSH доступу

#### Налаштування паролів:

**Enable Secret:**
```cisco
Router(config)# enable secret MySecurePassword123
```

**Enable Password (застарілий):**
```cisco
Router(config)# enable password MyPassword123
```

**Console Password:**
```cisco
Router(config)# line console 0
Router(config-line)# password ConsolePass123
Router(config-line)# login
Router(config-line)# exit
```

**VTY Password:**
```cisco
Router(config)# line vty 0 4
Router(config-line)# password VTYPass123
Router(config-line)# login
Router(config-line)# exit
```

#### Захист паролів:

**1. Service Password-Encryption:**
```cisco
Router(config)# service password-encryption
```
- Шифрує паролі в конфігурації
- Захищає від перегляду конфігурації

**2. Складні паролі:**
- Мінімум 8 символів
- Комбінація букв, цифр, символів
- Уникати звичайних слів

**3. Регулярна зміна:**
- Змінювати паролі регулярно
- Не використовувати однакові паролі

---

### 6.2.2 SSH замість Telnet

**SSH (Secure Shell)** - безпечний протокол для віддаленого доступу.

#### Чому SSH замість Telnet:

| Характеристика | Telnet | SSH |
|----------------|--------|-----|
| Шифрування | Ні | Так |
| Аутентифікація | Пароль (plain text) | Ключі + пароль |
| Безпека | Низька | Висока |
| Порт | 23 | 22 |

#### Налаштування SSH:

**Крок 1: Налаштування hostname та domain name:**
```cisco
Router(config)# hostname R1
R1(config)# ip domain-name example.com
```

**Крок 2: Генерація ключів:**
```cisco
R1(config)# crypto key generate rsa
How many bits in the modulus [512]: 1024
```

**Крок 3: Налаштування VTY для SSH:**
```cisco
R1(config)# username admin secret AdminPass123
R1(config)# line vty 0 4
R1(config-line)# transport input ssh
R1(config-line)# login local
R1(config-line)# exit
```

**Крок 4: Налаштування SSH версії (опціонально):**
```cisco
R1(config)# ip ssh version 2
```

**Крок 5: Налаштування таймаутів (опціонально):**
```cisco
R1(config)# ip ssh time-out 60
R1(config)# ip ssh authentication-retries 3
```

#### Вимкнення Telnet:

```cisco
R1(config)# line vty 0 4
R1(config-line)# transport input ssh
R1(config-line)# exit
```

**Або повне вимкнення:**
```cisco
R1(config)# no ip telnet
```

#### Перевірка SSH:

```cisco
# Перегляд SSH конфігурації
show ip ssh

# Перегляд активних SSH сесій
show users
```

#### Підключення через SSH:

**З клієнта:**
```bash
ssh admin@192.168.1.1
```

---

### 6.2.3 Відключення невикористовуваних сервісів

#### Небезпечні сервіси:

**1. CDP (Cisco Discovery Protocol):**
- Розкриває інформацію про пристрій
- Використовується для збору інформації

**Вимкнення:**
```cisco
Router(config)# no cdp run
```

**Або на конкретному інтерфейсі:**
```cisco
Router(config-if)# no cdp enable
```

**2. HTTP Server:**
- Веб-інтерфейс для управління
- Може бути небезпечним

**Вимкнення:**
```cisco
Router(config)# no ip http server
Router(config)# no ip http secure-server
```

**3. Finger:**
- Застарілий сервіс
- Розкриває інформацію про користувачів

**Вимкнення:**
```cisco
Router(config)# no service finger
```

**4. Small Servers:**
- Echo, Discard, Chargen
- Можуть використовуватися для DoS

**Вимкнення:**
```cisco
Router(config)# no service tcp-small-servers
Router(config)# no service udp-small-servers
```

**5. IP Source Routing:**
- Можливість вказати маршрут
- Може використовуватися для атак

**Вимкнення:**
```cisco
Router(config)# no ip source-route
```

**6. IP Redirects:**
- ICMP redirects
- Можуть використовуватися для MITM

**Вимкнення (на інтерфейсі):**
```cisco
Router(config-if)# no ip redirects
```

#### Рекомендовані налаштування:

```cisco
! Вимкнути небезпечні сервіси
no cdp run
no ip http server
no ip http secure-server
no service finger
no service tcp-small-servers
no service udp-small-servers
no ip source-route

! На інтерфейсах
interface range FastEthernet0/0-23
no cdp enable
no ip redirects
exit
```

---

### 6.2.4 Безпека комутаторів (Port Security)

**Port Security** - функція безпеки комутаторів, яка обмежує MAC-адреси на порту.

#### Призначення:

- Захист від несанкціонованого підключення
- Обмеження кількості MAC-адрес на порту
- Захист від MAC flooding атак

#### Типи Port Security:

**1. Static Secure MAC:**
- MAC-адреси налаштовуються вручну
- Постійні, не видаляються

**2. Dynamic Secure MAC:**
- MAC-адреси вивчаються автоматично
- Видаляються при вимкненні порту

**3. Sticky Secure MAC:**
- MAC-адреси вивчаються автоматично
- Зберігаються в конфігурації
- Не видаляються при перезавантаженні

#### Налаштування Port Security:

**Базове налаштування:**
```cisco
Switch(config)# interface FastEthernet0/1
Switch(config-if)# switchport mode access
Switch(config-if)# switchport port-security
Switch(config-if)# switchport port-security maximum 2
Switch(config-if)# switchport port-security violation restrict
Switch(config-if)# exit
```

**З sticky MAC:**
```cisco
Switch(config)# interface FastEthernet0/1
Switch(config-if)# switchport mode access
Switch(config-if)# switchport port-security
Switch(config-if)# switchport port-security maximum 2
Switch(config-if)# switchport port-security mac-address sticky
Switch(config-if)# switchport port-security violation shutdown
Switch(config-if)# exit
```

**З статичними MAC:**
```cisco
Switch(config)# interface FastEthernet0/1
Switch(config-if)# switchport mode access
Switch(config-if)# switchport port-security
Switch(config-if)# switchport port-security maximum 2
Switch(config-if)# switchport port-security mac-address 0001.0001.0001
Switch(config-if)# switchport port-security mac-address 0002.0002.0002
Switch(config-if)# exit
```

#### Типи порушень (Violation):

**1. Protect:**
- Забороняє нові MAC-адреси
- Не логує події
- Найменш обмежувальний

**2. Restrict:**
- Забороняє нові MAC-адреси
- Логує події
- Надсилає SNMP trap

**3. Shutdown:**
- Вимикає порт
- Логує події
- Надсилає SNMP trap
- Потрібно вручну увімкнути порт
- Найбезпечніший

#### Налаштування aging:

**Статичне старіння:**
```cisco
Switch(config-if)# switchport port-security aging time 60
Switch(config-if)# switchport port-security aging type absolute
```

**Або інкрементальне старіння:**
```cisco
Switch(config-if)# switchport port-security aging type inactivity
```

#### Перевірка Port Security:

```cisco
# Перегляд Port Security на всіх портах
show port-security

# Перегляд конкретного порту
show port-security interface FastEthernet0/1

# Перегляд MAC-адрес
show port-security address
```

#### Приклад виводу:

```
Switch# show port-security interface FastEthernet0/1
Port Security              : Enabled
Port Status                : Secure-up
Violation Mode             : Shutdown
Maximum MAC Addresses      : 2
Total MAC Addresses        : 2
Configured MAC Addresses   : 0
Sticky MAC Addresses       : 2
Last Source Address:Vlan   : 0001.0001.0001:1
Security Violation Count   : 0
```

#### Відновлення порту після shutdown:

```cisco
Switch(config)# interface FastEthernet0/1
Switch(config-if)# shutdown
Switch(config-if)# no shutdown
Switch(config-if)# exit
```

---

## 🛠️ Практичні завдання

### Завдання 1: Налаштування Standard ACL

Створіть ACL, який:
- Забороняє весь трафік з 192.168.1.0/24
- Дозволяє весь інший трафік

**Рішення:**
```cisco
Router(config)# access-list 10 deny 192.168.1.0 0.0.0.255
Router(config)# access-list 10 permit any
Router(config)# interface FastEthernet0/0
Router(config-if)# ip access-group 10 in
Router(config-if)# exit
```

### Завдання 2: Налаштування Extended ACL

Створіть ACL для веб-сервера:
- Дозволити HTTP (port 80) з будь-якої мережі
- Дозволити HTTPS (port 443) з будь-якої мережі
- Дозволити SSH (port 22) тільки з 192.168.1.0/24
- Заборонити все інше

**Рішення:**
```cisco
Router(config)# access-list 100 permit tcp any host 192.168.1.10 eq 80
Router(config)# access-list 100 permit tcp any host 192.168.1.10 eq 443
Router(config)# access-list 100 permit tcp 192.168.1.0 0.0.0.255 host 192.168.1.10 eq 22
Router(config)# access-list 100 deny ip any any
Router(config)# interface FastEthernet0/0
Router(config-if)# ip access-group 100 in
Router(config-if)# exit
```

### Завдання 3: Налаштування SSH

Налаштуйте SSH на маршрутизаторі:
- Hostname: R1
- Domain: company.com
- Username: admin
- RSA ключ: 1024 біти

**Рішення:**
```cisco
Router(config)# hostname R1
R1(config)# ip domain-name company.com
R1(config)# crypto key generate rsa
How many bits in the modulus [512]: 1024
R1(config)# username admin secret AdminPass123
R1(config)# line vty 0 4
R1(config-line)# transport input ssh
R1(config-line)# login local
R1(config-line)# exit
R1(config)# ip ssh version 2
```

### Завдання 4: Налаштування Port Security

Налаштуйте Port Security на порту Fa0/1:
- Максимум 2 MAC-адреси
- Sticky MAC
- Violation: shutdown

**Рішення:**
```cisco
Switch(config)# interface FastEthernet0/1
Switch(config-if)# switchport mode access
Switch(config-if)# switchport port-security
Switch(config-if)# switchport port-security maximum 2
Switch(config-if)# switchport port-security mac-address sticky
Switch(config-if)# switchport port-security violation shutdown
Switch(config-if)# exit
```

---

## 📝 Тести для самоперевірки

### Тест 1: Основи безпеки

**Питання 1:** Що означає CIA в контексті безпеки?
- [ ] A) Central Intelligence Agency
- [ ] B) Confidentiality, Integrity, Availability
- [ ] C) Computer Internet Access
- [ ] D) Cyber Information Assurance

**Питання 2:** Яка атака спрямована на недоступність сервісу?
- [ ] A) MITM
- [ ] B) DoS
- [ ] C) Phishing
- [ ] D) Malware

**Питання 3:** Який протокол безпечніший для віддаленого доступу?
- [ ] A) Telnet
- [ ] B) SSH
- [ ] C) HTTP
- [ ] D) FTP

**Відповіді:**
1. B) Confidentiality, Integrity, Availability
2. B) DoS (Denial of Service)
3. B) SSH

### Тест 2: ACL

**Питання 1:** Який діапазон номерів для Standard ACL?
- [ ] A) 1-99
- [ ] B) 100-199
- [ ] C) 1-199
- [ ] D) 1-99, 1300-1999

**Питання 2:** Що робить implicit deny в ACL?
- [ ] A) Дозволяє весь трафік
- [ ] B) Забороняє весь трафік, який не збігся з правилами
- [ ] C) Логує всі події
- [ ] D) Нічого

**Відповіді:**
1. D) 1-99, 1300-1999
2. B) Забороняє весь трафік, який не збігся з правилами

### Тест 3: Безпека пристроїв

**Питання 1:** Який тип Port Security violation найбезпечніший?
- [ ] A) Protect
- [ ] B) Restrict
- [ ] C) Shutdown
- [ ] D) Всі однакові

**Питання 2:** Який порт використовує SSH?
- [ ] A) 21
- [ ] B) 22
- [ ] C) 23
- [ ] D) 80

**Відповіді:**
1. C) Shutdown
2. B) 22

---

## 📚 Додаткові ресурси для вивчення

### Відео матеріали:
- [ ] Jeremy's IT Lab - "CCNA Security"
- [ ] NetworkChuck - "Network Security"
- [ ] Professor Messer - "Network+ Security"

### Практика:
- [ ] Налаштування ACL в Packet Tracer
- [ ] Налаштування SSH на реальному пристрої
- [ ] Налаштування Port Security на комутаторі

### Документація:
- [ ] Cisco Security Configuration Guide
- [ ] OWASP Top 10
- [ ] NIST Cybersecurity Framework

---

## ✅ Чеклист завершення тижня 11-12

### Теоретичні знання:
- [ ] Розумію принципи безпеки (CIA Triad)
- [ ] Знаю основні типи атак та способи захисту
- [ ] Розумію призначення файрволів
- [ ] Знаю типи ACL та їх використання
- [ ] Розумію основи VPN
- [ ] Знаю як захищати паролі
- [ ] Розумію переваги SSH над Telnet
- [ ] Знаю які сервіси потрібно вимикати
- [ ] Розумію Port Security та його типи

### Практичні навички:
- [ ] Можу налаштувати Standard ACL
- [ ] Можу налаштувати Extended ACL
- [ ] Можу налаштувати SSH
- [ ] Можу налаштувати Port Security
- [ ] Можу вимкнути небезпечні сервіси
- [ ] Пройшов тести для самоперевірки з результатом 80%+

---

## 🎯 Наступні кроки

Після завершення тижня 11-12 переходимо до:
- **Тиждень 13-14:** Бездротові мережі та додаткові теми
- Wi-Fi стандарти
- VLAN та Trunking
- QoS основи

**Успіхів у навчанні! 🚀**

*Останнє оновлення: 2024*

